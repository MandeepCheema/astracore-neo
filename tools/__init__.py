"""AstraCore Neo — internal toolchain.

Subpackages:
- ``npu_ref``: compute reference (compiler, quantiser, runtime, golden mirrors).
- ``safety``: ISO 26262 functional-safety tooling (FMEDA, fault injection).
- ``frontends``: PyTorch / TVM / XLA / MLIR / NNEF model frontends.
"""
